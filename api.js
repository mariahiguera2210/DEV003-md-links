const viewInfo = (userName, userAge) => `el nombre de la persona es ${userName} y su edad es ${userAge}`


function viewInfo(userName, userAge){
    return `el nombre de la persona es ${userName} y su edad es ${userAge}`
}

console.log(viewInfo('maria', 30))

// exists sync 
